const primaryColor = "#1B7991";

const theme = {
  fontFamily: "Helvetica",
  textColor: "#202020",
  linkColor: primaryColor,
  button: {
    fontFamily: "Helvetica",
    primary: {
      color: primaryColor
    }
  },
  footer: {
    textColor: "#757575"
  }
};

export default theme;

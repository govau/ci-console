---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: govau/cga-cf-bosh-cli
    tag: latest

inputs:
- name: build

outputs:
- name: result

params:
- DOMAIN

run:
  path: govau-ci-console/ci/prepare-manifest.sh
